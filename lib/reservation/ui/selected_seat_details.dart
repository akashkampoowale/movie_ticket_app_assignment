import 'package:flutter/material.dart';

class SelectedSeatsDetails extends StatelessWidget {
  const SelectedSeatsDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    //todo add details
    return Container();
  }
}
